# Jetson Orin Nano 风格转换设备依赖
# 安装顺序: pip install -r requirements.txt

# 核心依赖
numpy>=1.21.0,<2.0
opencv-python>=4.5.0,<5.0
Pillow>=9.0.0

# ONNX Runtime (GPU 加速推理)
# Jetson 上安装: pip install onnxruntime-gpu
# 其他平台: pip install onnxruntime-gpu 或 pip install onnxruntime
onnxruntime-gpu

# ONNX 模型工具
onnx>=1.16.0

# 工具库
tqdm>=4.60.0
requests>=2.28.0

# TensorRT (通过 apt 安装，Jetson 上通常已预装)
# sudo apt install tensorrt python3-libnvinfer
